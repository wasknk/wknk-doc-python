

count(start, step)
cycle(iterable)
repeat(object, [times])


chain(a, b, c)
zip_longest(a, b, fillvalue=None)
compress(data, selectors)
islice(iterable, start, stop, step)

product(a, b)
permutations(a, k)
combinations(a, k)